import type { Metadata } from "next";
import "./globals.css";
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "FreshCart — Fresh Groceries, Smart Substitutions & Express 30-Min Delivery",
  description: "Order farm-fresh vegetables, organic milk, daily grocery staples with FEFO freshness guarantee and scheduled delivery slots.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="flex flex-col min-h-screen">
        <Navbar />
        <main className="flex-grow">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
